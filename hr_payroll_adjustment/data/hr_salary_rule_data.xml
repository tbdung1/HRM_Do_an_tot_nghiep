<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Salary Rule Category for Adjustments -->
        <record id="hr_salary_rule_category_adjustment" model="hr.salary.rule.category">
            <field name="name">Trợ cấp - Khấu trừ</field>
            <field name="code">ADJUSTMENT</field>
        </record>

        <!-- Salary Rule: Bonus (Thưởng) -->
        <!-- <record id="hr_salary_rule_bonus" model="hr.salary.rule">
            <field name="name">Thưởng trong tháng</field>
            <field name="code">BONUS</field>
            <field name="category_id" ref="hr_salary_rule_category_adjustment"/>
            <field name="sequence">200</field>
            <field name="condition_select">python</field>
            <field name="condition_python">result = worked_days.WORK100 and contract.employee_id.get_work_days_data(payslip.date_from, payslip.date_to).get('bonus_amount', 0) > 0</field>
            <field name="amount_select">code</field>
            <field name="amount_python_compute">result = contract.employee_id.get_work_days_data(payslip.date_from, payslip.date_to).get('bonus_amount', 0)</field>
            <field name="appears_on_payslip">True</field>
        </record> -->

        <!-- Salary Rule: Penalty (Phạt) -->
        <!-- <record id="hr_salary_rule_penalty" model="hr.salary.rule">
            <field name="name">Phạt trong tháng</field>
            <field name="code">PENALTY</field>
            <field name="category_id" ref="hr_salary_rule_category_adjustment"/>
            <field name="sequence">201</field>
            <field name="condition_select">python</field>
            <field name="condition_python">result = worked_days.WORK100 and contract.employee_id.get_work_days_data(payslip.date_from, payslip.date_to).get('penalty_amount', 0) > 0</field>
            <field name="amount_select">code</field>
            <field name="amount_python_compute">result = -contract.employee_id.get_work_days_data(payslip.date_from, payslip.date_to).get('penalty_amount', 0)</field>
            <field name="appears_on_payslip">True</field>
        </record> -->

    </data>
</odoo>
