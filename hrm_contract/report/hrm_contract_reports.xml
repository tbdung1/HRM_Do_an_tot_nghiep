<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_report_hrm_contract_draft_pdf" model="ir.actions.report">
        <field name="name">Hợp đồng lao động (Draft)</field>
        <field name="model">hrm.contract.draft</field>
        <field name="report_type">qweb-html</field>
        <field name="report_name">hrm_contract.report_hrm_contract_draft_pdf</field>
        <field name="report_file">hrm_contract.report_hrm_contract_draft_pdf</field>
        <field name="print_report_name">'HDLD_' + (object.name or 'contract_draft')</field>
        <field name="paperformat_id" ref="base.paperformat_euro" />
        <field name="binding_model_id" ref="model_hrm_contract_draft" />
        <field name="binding_type">report</field>
    </record>

</odoo>
